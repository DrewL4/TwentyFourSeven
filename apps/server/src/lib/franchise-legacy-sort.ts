/** Legacy channel.autoSortMethod values from before timeline:<slug> format. */
export const LEGACY_TIMELINE_SORT_ALIASES: Record<string, string> = {
  'timeline-mcu': 'mcu-chronological',
  'timeline-star-wars': 'star-wars-saga',
};
